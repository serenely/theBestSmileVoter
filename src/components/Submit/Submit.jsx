import './Submit.css'

const Submit = ({showResults}) => {
    return(
        <div >
            {}
            <button className='btn__submit' onClick={showResults}>Submit</button>
        </div>
    )
}

export default Submit;